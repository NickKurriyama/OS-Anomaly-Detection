# OS-Anomaly-Detection
Using Machine Learning to realize "low-level system activity from a Linux-based environment, recorded via kernel-level monitoring. Each row reflects a snapshot of process behavior or a system call execution — providing a goldmine for detecting subtle anomalies in OS-level operations." 
